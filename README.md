# MyWebApp
ASP.NET Core MVC sample project generated by ChatGPT.

## How to run

1. Install .NET 8 SDK (or change TargetFramework in csproj to your installed SDK).
2. From project folder:
   dotnet restore
   dotnet run

The app uses SQLite file `app.db` created automatically in the project folder.
